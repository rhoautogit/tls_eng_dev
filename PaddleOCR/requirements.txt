pdfplumber>=0.10.0
opencv-python>=4.8.0
PyMuPDF>=1.23.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.65.0
pytest>=7.4.0
pytest-cov>=4.0.0

# OCR (PaddleOCR) -- GPU setup (recommended, requires Python 3.12 venv)
# Install from PaddlePaddle official source for CUDA 12 build:
#   pip install paddlepaddle-gpu==2.6.1.post120 -f https://www.paddlepaddle.org.cn/whl/windows/mkl/avx/stable.html
#   pip install paddleocr==2.9.1 "nvidia-cudnn-cu12<9"
# CPU fallback (Python 3.13 compatible):
#   pip install paddlepaddle==3.2.0 paddleocr==3.3.0
paddleocr==2.9.1

# Qwen-VL verification runs via Ollama (no Python torch dependency needed)
# Install Ollama separately: https://ollama.ai
# Then: ollama pull qwen3-vl:8b
